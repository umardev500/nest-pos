{
  "extends": "./tsconfig.json",
  "exclude": [
    "node_modules",
    "test",
    "dist",
    "**/*spec.ts",
    "temp/**", // ✅ This excludes all subfolders and files inside temp/
    "src/infra/prisma/seeders/**"
  ]
}
