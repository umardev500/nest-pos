### Get All Orders
# Retrieve all orders for the current merchant.

GET {{API_URL}}/orders


### Get Order by ID
# Fetch a single order by its ID.

GET {{API_URL}}/orders/2


### Create Order
# Create a new order for a merchant.

POST {{API_URL}}/orders
Content-Type: application/json

{
  "merchant_id": 1,
  "order_type_id": 2,
  "total_amount": 100,
  "discount_type": "PERCENT",
  "discount_value": 10,
  "order_items": [
    {
      "product_id": 1,
      "unit_id": 1,
      "quantity": 2,
      "price": 20,
      "subtotal": 40
    },
    {
      "product_id": 2,
      "unit_id": 1,
      "quantity": 1,
      "price": 60,
      "subtotal": 60
    }
  ]
}


### Update Order
# Update an existing order by ID.

PUT {{API_URL}}/orders/2
Content-Type: application/json

{
  "status": "PAID",
  "discount_value": 15,
  "order_items": [
    {
      "product_id": 2,
      "unit_id": 1,
      "quantity": 3,
      "price": 1000,
      "subtotal": 60
    }
  ]
}


### Delete Order
# Delete an order by ID.

DELETE {{API_URL}}/orders/1
